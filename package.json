{
  "name": "eagle-video-downloader",
  "version": "2.4.0",
  "description": "Download video links supported by yt-dlp and import them into Eagle",
  "main": "js/plugin.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "package": "npm run build && rimraf bin/ && node -e \"const zip = new (require('adm-zip'))(); zip.addLocalFolder('Plugin'); zip.writeZip('视频下载器.eagleplugin');\""
  },
  "keywords": [
    "eagle",
    "plugin",
    "video-downloader",
    "yt-dlp"
  ],
  "author": "fansanqiu",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "i18next": "^23.8.2"
  }
}
